load("//:defs.bzl", "ody_rust_crate")

exports_files(
    ["repo_root.marker"],
    visibility = ["//visibility:public"],
)

ody_rust_crate(
    name = "cargo-bin",
    compile_data = ["repo_root.marker"],
    crate_name = "ody_utils_cargo_bin",
    lib_data_extra = ["repo_root.marker"],
    rustc_env = {
        "ODY_REPO_ROOT_MARKER": "$(rlocationpath :repo_root.marker)",
    },
    test_data_extra = ["repo_root.marker"],
)
