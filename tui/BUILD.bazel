load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "ody_rust_crate")

ody_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//ody/collaboration-mode-templates:templates/default.md",
        "//ody/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "ody_tui",
    extra_binaries = [
        "//ody/cli:ody",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
