{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AuthMode": {
      "description": "Authentication mode for model providers.",
      "oneOf": [
        {
          "description": "API key provided by the caller and stored by Ody.",
          "enum": [
            "apikey"
          ],
          "type": "string"
        },
        {
          "description": "No authentication.",
          "enum": [
            "unauthenticated"
          ],
          "type": "string"
        }
      ]
    }
  },
  "properties": {
    "authMode": {
      "anyOf": [
        {
          "$ref": "#/definitions/AuthMode"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "title": "AuthUpdatedNotification",
  "type": "object"
}