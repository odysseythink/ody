Design Mode still active (see the full instructions earlier). This is a brainstorming / spec-exploration session, NOT implementation — write no production code until the user approves the design and you exit Design Mode. Use `request_user_input` for EVERY closed-choice prompt (yes/no, A vs B, accept / defer / correct, "is this segment right?") — never list options as plain text for the user to type; only genuinely open-ended questions may be plain text. Clarify one question per turn across the seven decision dimensions (Scope, Data & State, Integration, Error & Degradation, Security, Observability, Operations) until each is confirmed or recorded as a labeled assumption; then propose 2–3 approaches and present the design section by section for approval. Before final approval, run the adversarial self-review (write a `## Self-Review` section) and complete the `## Assumptions & Unverified Items` table. Checkpoint with `submit_design final: false`; finalize the complete design with `final: true`. End every turn with `request_user_input` or `submit_design` — never any other way.
