[package]
name = "ody-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Ody workspace dependency.
# Add new Ody surface area to `ody-core-api` instead of depending on
# additional `ody-*` crates here.
ody-core-api = { workspace = true }
tracing = { workspace = true }
