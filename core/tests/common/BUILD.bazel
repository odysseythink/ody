load("//:defs.bzl", "ody_rust_crate")

ody_rust_crate(
    name = "common",
    crate_name = "core_test_support",
    crate_srcs = glob(["*.rs"]),
    deps_extra = [
        "@crates//:similar",
    ],
)
